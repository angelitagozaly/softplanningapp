<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SoftPlanningApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color (your sage-blue) -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Background -->
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>

        <!-- Surface (cards, sheets, etc.) -->
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/statusBarColor</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/windowBackground</item>

        <!-- Error colors -->
        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/white</item>

        <!-- Additional Material Design attributes -->
        <item name="colorControlNormal">@color/textSecondary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/rippleColor</item>

        <!-- Text selection -->
        <item name="android:textColorHighlight">@color/highlightColor</item>
    </style>

    <!-- Card Theme -->
    <style name="CardViewStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/cardBackground</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="contentPadding">16dp</item>
    </style>

    <!-- Button Styles -->
    <style name="ButtonPrimary" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/buttonPrimary</item>
        <item name="android:textColor">@color/textOnColored</item>
        <item name="rippleColor">@color/rippleColor</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="ButtonSecondary" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="backgroundTint">@color/buttonSecondary</item>
        <item name="android:textColor">@color/buttonSecondaryText</item>
        <item name="strokeColor">@color/borderLight</item>
        <item name="rippleColor">@color/rippleColor</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- FAB Style -->
    <style name="FabStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/fabBackground</item>
        <item name="tint">@color/fabIcon</item>
        <item name="rippleColor">@color/rippleColor</item>
    </style>

    <!-- Text Input Layout Style -->
    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/inputFocused</item>
        <item name="hintTextColor">@color/textMuted</item>
        <item name="android:textColorHint">@color/textHint</item>
    </style>

    <!-- Toolbar Style -->
    <style name="ToolbarStyle" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextColor">@color/textOnColored</item>
        <item name="subtitleTextColor">@color/textOnColored</item>
    </style>
</resources>